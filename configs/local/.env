DJANGO_SETTINGS_MODULE=settings
DJANGO_SECRET_KEY=local-dev-secret-not-for-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,136.118.94.124,10.138.0.2

# ── Supabase ─────────────────────────────────────────────────────────────────
# Get these from the Supabase dashboard: Project Settings → API.
# SUPABASE_ANON_KEY is safe to expose to the browser (it's gated by RLS).
# SUPABASE_JWT_SECRET must stay on the server — it's used to verify auth tokens.
SUPABASE_URL=https://ijyxftzwcdelzlbjgssm.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_JWT_SECRET=